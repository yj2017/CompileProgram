DATAS SEGMENT
    ;此处输入数据段代码  
    ARRAY DW -30H,30H,39H
    COUNT DB 30H
DATAS ENDS

STACKS SEGMENT
    ;此处输入堆栈段代码
STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
    MOV AX,DATAS
    MOV DS,AX
    ;此处输入代码段代码
    MOV DI,OFFSET ARRAY
    MOV AX,[DI]
    INC DI
    MOV BX,[DI]
    INC DI
    MOV CX,[DI]
    CMP AX,CX
    JNZ COMPnext1
    INC COUNT
COMPnext1:
    CMP AX,BX
    JNZ  COMPnext2
    INC COUNT
COMPnext2:
    CMP CX,BX
    JNZ PRINT
	INC COUNT 
	JMP PRINT
PRINT:
    CMP COUNT,31H
	JZ INPUT1
	CMP COUNT,33H
	JZ INPUT2
	CMP COUNT,30H
	JZ INPUT0	
  
INPUT1:  
	MOV DL,31H
	MOV AH,02H
	INT 21H
	JMP EXIT
INPUT2:
	MOV DL,32H
	MOV AH,02H
	INT 21H
	JMP EXIT
INPUT0:
	MOV DL,30H
	MOV AH,02H
	INT 21H 
	JMP EXIT
EXIT:
 MOV AH,4CH
    INT 21H	


CODES ENDS
    END START




