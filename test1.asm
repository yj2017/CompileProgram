DATAS SEGMENT
;此处输入数据段代码 
 
DATAS ENDS

STACKS SEGMENT
;此处输入堆栈段代码
STACKS ENDS

;实现AX二进制数转成十六进制数，并输出
CODES SEGMENT
	ASSUME CS:CODES,DS:DATAS,SS:STACKS
	START:
	MOV AX,DATAS
	MOV DS,AX
	;此处输入代码段代码
	
	MOV CH,4
	mov cl,4
	MOV BX,1A00H
LABEL1: 
	MOV DL,0FH
	ROL BX,cl
	AND DL,BL
	ADD DL,30H
	CMP DL,3aH
	JL LABEL2
	ADD DL,07H
LABEL2:
	MOV AH,2
	INT 21H
	DEC CH
	JNZ LABEL1 
	
	MOV AH,4CH
	INT 21H
CODES ENDS
END START


